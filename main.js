const editSocialButtons = () => {
    var btns = document.querySelectorAll('button[class=accountButton]');
    for (var i = 0; i < btns.length; i++) {
        console.log(btns[i]);
    }
}

editSocialButtons();
